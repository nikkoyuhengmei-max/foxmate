# Core (required to run data + backtest + risk + compliance + performance)
numpy>=1.23
pandas>=1.5

# Web dashboard + REST API (optional, `pip install -e .[web]`)
fastapi>=0.110
uvicorn>=0.27
jinja2>=3.1

# Machine learning / forecasting (optional, `pip install -e .[ml]`)
scikit-learn>=1.2

# Charting (optional)
matplotlib>=3.6
